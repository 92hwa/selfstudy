<Window x:Class="wpfEx02.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:wpfEx02.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <StackPanel Orientation="Vertical"  HorizontalAlignment="Left" Margin="10">
            <Button Content="Load" Command="{Binding LoadCommand}" Width="150" Height="30" Margin="0, 0, 0, 20" />
        </StackPanel>

        <Image Source="{Binding Image}" Margin="170, 10, 10, 10" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
    </Grid>
</Window>
